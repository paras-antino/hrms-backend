# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=admin
DB_NAME=hrms_db

# App
APP_ENV=development
CORS_ORIGINS=http://localhost:5173

# Async Database URL (constructed in config.py if not provided)
# DATABASE_URL=postgresql+asyncpg://postgres:admin@localhost:5432/hrms_db
  